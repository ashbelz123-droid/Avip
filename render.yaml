services:
  - type: worker
    name: betpawa-ghost-v2
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python ghost.py
    plan: free
    envVars:
      - key: PHONE
        value: "+256700000000"
      - key: PYTHONUNBUFFERED
        value: "1"
